---
name: ziwei-skill
description: >
  确定性紫微斗数排盘、校盘与传统文化分析。Use when the user asks for 紫微斗数、紫微排盘、紫微命盘、
  命宫、身宫、十二宫、十四主星、四化、三方四正、大限、流年、事业、财运、身心健康、感情、学业、
  合盘、出生时辰反推或跨软件校盘。Supports solar/lunar dates, leap months, 13 birth-time candidates,
  common and Zhongzhou algorithms, Python automation, machine-readable output, and clearly separated optional
  cross-references to user-supplied Bazi, astrology, feng-shui, personality, face/palm, or divination material.
---

# 紫微斗数排盘与解读

使用确定性脚本排盘。不要让语言模型口算命身宫、星曜、四化或运限。

## 定位 Skill 根目录

所有脚本和引用路径都相对当前 `SKILL.md` 所在目录解析，不相对用户项目的工作目录解析：

- Codex 使用技能列表中提供的 `SKILL.md` 路径定位根目录。
- Claude Code 使用当前 Skill 路径；可用 `${CLAUDE_SKILL_DIR}` 引用该目录。

运行命令时把 Skill 根目录设为工作目录，或使用脚本的绝对路径。不要因为用户项目中没有 `scripts/`
就改用语言模型口算，也不要把排盘脚本复制进用户项目。

## 收集并确认信息

收集：

1. 公历或农历出生日期；农历注明是否闰月。
2. 精确出生时间或时辰；没有时辰时停止排盘。
3. 性别。
4. 出生城市；时间靠近时辰边界时用于真太阳时核对。
5. 分析主题：整体、事业、财运、身体健康、心理状态、感情、学业、某年运势、合盘或校时。
6. 与主题直接相关的现实背景；例如职业阶段、收支结构、学习阶段、已知身心情况或关系状态。

复述关键信息并允许用户修正。已有完整信息时直接继续，不重复询问姓名等无关字段。只收集完成任务所需的最少信息，默认当事人在世，当前日期取系统日期。

缺少出生时辰且用户并未请求校时时，停止排盘。用户请求校时时，收集可核验的经历节点与大致时间范围，再运行 13 个候选时辰比较；不得先猜一个时辰。

## 解读前须知

在首次实质解读前简要说明：

- 紫微斗数、八字、占星、风水、面相、手相与占卜不是经科学验证的预测技术，只能作为传统文化研究、反思或娱乐视角。
- 医疗、心理、法律、投资、升学、就业和关系等现实决策必须依据专业意见与可验证信息；不得用命理结论替代。
- 不索取姓名、证件号、详细住址等无关隐私。处理第三方出生资料、命盘或照片前，先确认已获得对方许可。
- 不使用恐吓、宿命或依赖性措辞。把“反噬”等说法标为民俗信念或心理负担，不描述为已证实风险。

完整的隐私、同意、占卜和防巴纳姆效应规则见 `references/supplementary.md`。

## 安装依赖

若 Skill 根目录下的 `node_modules/iztro` 不存在，在该目录运行：

```bash
npm install
```

依赖固定为 MIT 开源的 `iztro@2.6.0`。Python 自动化入口使用 Python 3.10+ 标准库，不增加第三方 Python 依赖。不调用外部算命 API，不要求用户提供 API Key。

## 排盘

日常任务优先运行 Python 自动化入口。它调用锁定的 Node/iztro 引擎完成排盘，再自动整理生年四化、文昌文曲、红鸾天喜及六大主题的宫位事实包：

```bash
python3 scripts/ziwei_auto.py --solar 2000-08-16 --hour 03:30 --sex 女 --target-date 2026-08-28
python3 scripts/ziwei_auto.py --solar 2000-08-16 --shichen 寅 --sex 女 --format json
```

省略 `--hour` 和 `--shichen` 时，Python 入口自动切换到 13 时辰候选比较。需要保存结果时添加 `--output result.md`。Python 只负责编排和提取事实，不重新实现或近似计算星曜。

## 确定性专题解盘

用户询问事业、财帛或姻缘时，优先运行规则解盘入口。它先调用锁定的排盘引擎，再按固定、可审查的
星曜词义与宫位矩阵输出“脚本事实／传统解释／现实建议／证据”；可输出 Markdown 或 JSON：

```bash
python3 scripts/ziwei_interpret.py --topic 事业 --solar 2000-08-16 --hour 03:30 --sex 女 --target-date 2026-08-28
python3 scripts/ziwei_interpret.py --topic 财帛 --solar 2000-08-16 --hour 03:30 --sex 女 --format json
python3 scripts/ziwei_interpret.py --topic 姻缘 --solar 2000-08-16 --hour 03:30 --sex 女
```

`--topic` 同时接受 `career`、`wealth`、`relationship` 及其中文别名。脚本要求确定的出生时间；时辰未知时，
先走 13 时辰校时流程，不得对未确定的候选盘直接解读。

姻缘合盘使用同一入口和第二组 `--partner-*` 参数。运行前确认对方已经许可使用其出生资料，并显式传入
`--partner-consent`：

```bash
python3 scripts/ziwei_interpret.py \
  --topic 合盘 \
  --solar 2000-08-16 --hour 03:30 --sex 女 --label 甲方 \
  --partner-solar 1998-12-20 --partner-hour 14:20 --partner-sex 男 --partner-label 乙方 \
  --partner-consent --target-date 2026-08-28
```

合盘依次比较双方夫妻宫、命宫与身宫的需求/表达、双方生年四化跨盘映射及低权重宫支合冲。不得把结果
改写成匹配分数、唯一正缘或必然婚期；双方真实意愿和现实相处证据始终优先。

## 底层排盘与校时

需要查看完整原始命盘或调试底层排盘时，直接运行 Node 入口。

优先使用公历与具体时间：

```bash
node scripts/ziwei_pan.mjs --solar 2000-08-16 --hour 03:30 --sex 女 --target-date 2025-01-01
```

农历与子时示例：

```bash
node scripts/ziwei_pan.mjs --lunar 2000-07-17 --shichen 寅 --sex 女
node scripts/ziwei_pan.mjs --lunar 2001-04-12 --leap --shichen 子 --zi late --sex 男
```

机器可读输出：

```bash
node scripts/ziwei_pan.mjs --solar 2000-08-16 --hour 03:30 --sex 女 --format json
```

出生时间未知并明确请求校时时，比较 13 个时辰索引：

```bash
node scripts/ziwei_time_compare.mjs --solar 2000-08-16 --sex 女
```

把比较结果与用户提供的已发生事件逐项核对。外貌、发旋、人格分类等只能作为低权重自述线索，不能据此确定时辰。

需要中州派时显式指定：

```bash
node scripts/ziwei_pan.mjs --solar 2000-08-16 --hour 03:30 --sex 女 --algorithm zhongzhou --astro-type heaven
```

运行 `node scripts/ziwei_pan.mjs --help` 查看全部参数。

## 使用排盘结果

将脚本输出的出生信息、命身宫、五行局、十二宫、十四主星、辅杂曜、生年四化、大限和目标日期运限作为唯一排盘依据。可重新排版，不得凭记忆移动星曜或覆盖四化。

Python 事实包中的顶层 `bodyPalace` 是身宫所在宫位的完整事实。事业、健康和整体主题还会在各自的
`topics.*.bodyPalace` 中携带同一信息；必须读取它，而不能只使用身宫地支或宫位名称。

出现时辰边界警告时，先核对真太阳时。无法核定时，分别运行相邻两个时辰，只说明结构差异，不把其中一个假定为事实。

按需读取：

- 排盘参数、算法口径、黄金用例或跨软件差异：`references/calculation.md`
- 宫位、主星、四化、运限及六大专题的解读矩阵：`references/interpretation.md`
- 八字、占星、风水、家庭命盘、面相手相、人格量表、占卜、找失物、隐私与伦理边界：`references/supplementary.md`

## 解读顺序

依次分析：

1. 命宫、身宫、命主、身主、五行局。
2. 命宫三方四正：命宫、迁移、财帛、官禄。
3. 生年禄、权、科、忌的落星与落宫。
4. 按主题调用对应宫位矩阵；不得省略文档指定的必看宫位。
5. 当前大限，再叠目标流年；目标年份必须传 `--target-date` 生成。
6. 综合其他相关宫位与现实背景，区分长期结构、阶段触发和用户事实。
7. 提出 3–5 个已发生年份或可核验主题供用户校准，再收窄未来判断。

不要单宫单星下结论。空宫借看对宫，并检查三方四正、辅曜、煞曜与四化。

## 六大解读模块

用户请求整体解读时依次覆盖以下六项；请求单一主题时只展开相关项，不机械凑齐其余模块：

1. 事业：以官禄宫为核心，务必同时看财帛、命宫、迁移和身宫。
2. 财运：以财帛宫为核心，务必同时看官禄、命宫、迁移，再看田宅、福德和相关四化。
3. 健康：分别写身体负荷与心理压力，以疾厄、福德为核心，并让用户已知情况优先于传统象征。
4. 感情：以夫妻宫为核心，结合福德、迁移、命宫、红鸾、天喜、四化和运限；涉及对方命盘须先确认许可。
5. 整体：综合三方四正、生年四化、大限、流年四化与其余相关宫位，给出一致主题、矛盾信息和现实建议。
6. 学业：检查文昌、文曲的落宫与状态、化科、命宫三方四正、福德、官禄及大限流年；区分学习方式、应试表现和阶段窗口。

各模块的完整观察顺序、禁止跳步项和输出要求见 `references/interpretation.md`。

## 补充体系与具体问题

保持紫微斗数为主轴。仅在用户主动提供或明确请求时使用八字、占星、风水、家庭成员命盘、面相手相、人格量表或占卜，并遵守：

- 先分别说明每套体系的输入、口径和结论，再比较共同主题与冲突；不得把多套体系混成一个“确定答案”。
- 八字必须来自可靠排盘结果；需要说明采用的换年、真太阳时等口径。可参考传统子平框架，但不得假装已查阅未提供的古籍原文。
- 卧室位置或家庭布局只作用户自愿提供的环境线索，不声称它能导致疾病、财富或成绩。
- 占卜只处理一个清晰问题，优先一个方法和用户提供的首次结果；不得反复起卦直到得到满意答案，也不得声称首次结果必然正确。
- 找失物先给现实搜索清单；用户明确要求时，才把其首次梅花易数结果作为娱乐性补充，不保证方位或找回结果。

## 对照其他软件

先收集对方软件名称、命宫、身宫、五行局、十四主星和四化截图。逐项比较：

- 通行版或中州派；
- 天盘、地盘或人盘；
- 四化与亮度表；
- 闰月处理；
- 正月初一或立春换年；
- 晚子时归日；
- 真太阳时。

若参数不同，报告“口径差异”，不要称任一方绝对错误。

## 验证

修改脚本、依赖或排盘口径后运行：

```bash
npm test
```

黄金用例与 13 时辰校时验证写在 `references/calculation.md`。测试失败时停止解盘，先修正排盘。

## 输出与安全

- 开头明确本次采用的算法、天盘类型和时间规则。
- 使用“脚本事实／用户事实／传统解释／综合推断”四类标签或等价措辞，避免把不同证据层级混在一起。
- 不使用恐吓性断语，不预测死亡、重病或必然灾祸。
- 健康内容分为身体与心理两部分，只谈压力和生活习惯象征；出现症状、危机或自伤风险时停止命理推断并建议及时寻求专业帮助。
- 不为高影响决定给出“应该投资、离婚、退学、辞职、停药”等命令式结论。
- 不把用户发在聊天中的 API Key 写入文件、命令或日志。
- 结尾说明：传统命理仅供文化研究与娱乐参考，人生决策及其后果应以现实信息和专业意见为准。
